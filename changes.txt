create backend.php and return the result
create getSuggestion a returns the suggestion in json format lke this
var data = [
    {
    "id": "id1",
    "name": "name1"},
{
    "id": "id2",
    "name": "name2"}
];
and use this code for option
$.each(data, function(i, option) {
    $('#sel').append($('<option/>').attr("value", option.id).text(option.name));
});

Take every datatype from database no things from static.
